# URL pubblico dell'API (il browser la chiama direttamente: CORS_ORIGINS nel backend deve contenere l'origine del frontend)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
# URL pubblico del sito, per sitemap, OpenGraph e canonical
NEXT_PUBLIC_SITE_URL=http://localhost:3000
